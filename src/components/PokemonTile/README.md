# PokemonTile

<!-- STORY -->

## Introduction

PokemonTile is an easy-to-use component.

## Usage

```javascript
import { PokemonTile } from 'some-package-name';
```

## Example use

```javascript
const myPage = props => {
  return (
    <main>
      <PokemonTile />
    </main>
  );
};
```

## Properties

- `className` - is the class name of the component

| propName  | propType | defaultValue | isRequired |
| --------- | -------- | ------------ | ---------- |
| className | string   | -            | -          |
